name: VM5 autosquash guard

on:
  pull_request:
    types: [opened]

permissions:
  contents: read

jobs:
  guard:
    runs-on: ubuntu-latest
    steps:
      - name: Old-head-only guard
        run: echo VM5_OLD_HEAD_GUARD_OK=1

