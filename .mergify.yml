pull_request_rules:
  - name: VM5 pull-requests-only fast-forward target-ref discriminator
    conditions:
      - title = VM5_PRONLY_FAST_FORWARD_20260916
    actions:
      merge:
        method: fast-forward
